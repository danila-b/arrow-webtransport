[package]
name = "server-http2"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["cors"] }
arrow-array = "54"
arrow-schema = "54"
arrow-ipc = "54"
bytes = "1"
